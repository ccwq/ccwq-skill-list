# Curation rules

只收录可复用、已验证的项目事实、长期决策、约束和可复现避坑。新结论若与既有记录等价则更新或合并；冲突、分组取舍、删除和负向评分必须讨论并确认。正文以可公开最小事实表述，删除凭据和个人信息。

Trim 候选是 `positive - negative < 0`，或 `negative >= 3 && negative >= positive`。证据不足只标注未证实并保留，不因旧、无关或暂未核验删除。
